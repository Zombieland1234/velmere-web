# C15-Q7: durable observed-event ordering (internal, NO_GO)

Base remote Q5 57a29cfccb9b5e3718ae89c8426e7155a0c086ee plus delivered local Q6 content tree 8e455d08a084a652e1125e2073706278c4eed719. Later remote Q6 377ac8b5 is a different candidate (different inbox contract and omitted joined test registration), not silently adopted or overwritten.

Adds the already registered payment watermark RPC, an immutable event identity binding, and per-subject observed state. The 6-kind priority policy is unchanged. Every 36 kind pair is compared at older/equal/newer timestamps (108 transitions in one table-driven test). SQL serializes event then subject, uses SECURITY INVOKER with fixed search_path, forced RLS, no anon/authenticated access. Service role is explicitly trusted and can mutate current state. Identity journal permits SELECT/INSERT, not UPDATE/DELETE. Installation is guarded for a disposable test database; this is not an applied migration.

Response validation binds input identity, previous/current states, kind/priority/terminal and recomputes the existing decision. It does not coerce string flags, invent reasons, or return rejected input as current. 25 actual-entrypoint fixtures: Q6 before 4/25, Q7 after 25/25. This is one boundary-failure family, not 21 remotely exploitable vulnerabilities. Eight existing joined Q6 tests now use actual ordering SQL, not an accept-all adapter; three additional joined tests cover stale new-ID delivery, ordering transport failure, and retry after observed-but-unapplied refund. External transports remain controlled; grants are fixtures, not purchased.

Initial focused 44/45 exposed cumulative injected transport failures opening the existing payment circuit breaker across independent tests. Per-scenario test-state reset fixed harness isolation, not runtime thresholds. First type check rejected direct assignment to readonly NODE_ENV; fixture setup fixed. Failed logs are preserved in delivery.

IMPORTANT LIMITS: a watermark records observation, not completion of a payment effect. Locks end at RPC commit. Concurrent external callbacks may still overlap; this is not exactly-once, a transactional outbox, reconciliation, or a paid Auth/Stripe E2E. Priorities are existing product policy, not Stripe ordering guarantees. The namespace is a single trusted Stripe account/mode per isolated database; multi-account/Connect binding, modes, retention and cleanup require separate design. Source, schema and app must deploy together; old RPC shapes fail closed. No production SQL, customers, real or test payments, outbound provider messages, sale switches, main/C13 merge, or UI/engine change.
