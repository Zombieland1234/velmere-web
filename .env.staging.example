# Unconfigured template. Never replace with an existing project's keys.
# Only fill after a NEW isolated project is created and its returned ref is recorded.
VELMERE_DEPLOYMENT_ENV=staging
VELMERE_STAGING_SUPABASE_PROJECT_REF=REPLACE_WITH_NEW_PROJECT_REF
NEXT_PUBLIC_SUPABASE_URL=https://REPLACE_WITH_NEW_PROJECT_REF.supabase.co
SUPABASE_URL=https://REPLACE_WITH_NEW_PROJECT_REF.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
# Keep Stripe disabled until a specific TEST account is chosen and reviewed.
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
